#include<stdio.h>
int main() {
    int count, num;
    int smallest, largrst;

    printf("enter number of element\n");
    scanf("%d",&count);

    if (count < 0) {
        printf("enter a valid number");
        return 1;
    }
    printf("enter %d numbers\n",count);
    scanf("%d",&num);
    smallest = largest = num;
    int (i<count) {
        scanf("%d",&num)

        if (nun<smallest) {
            smallest = largest = num;
            if (num>largest){
                largest = num;

            }
        }
        i++;


        
    }
    return 0;



}