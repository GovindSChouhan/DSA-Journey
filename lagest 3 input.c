#include<stdio.h>
int main() {
    int num1 ,num2 ,num3;
printf("enter three num");
scanf("%d%d%d",&num,&num,&num);

if (num1>=num2&&num1>=num3)
peintf("%d.2 if is the largest num\n");

if else (num2>=num1 && num2>=3)
printf("%d.2 if is the largest num\n");

else
printf("%d.2 if the largest num\n");
return 0;

}